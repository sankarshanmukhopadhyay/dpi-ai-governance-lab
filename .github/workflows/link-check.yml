name: Link Check
on:
  push:
  pull_request:
jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder link check
        run: |
          echo "TODO: add link checker"
